{
  "name": "cab",
  "private": true,
  "version": "0.6.0",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "vp dev",
    "dev:server": "cargo watch -c -w crates -w Cargo.toml -x \"run -p cab-server\"",
    "dev:server:once": "cargo run -p cab-server",
    "build": "vp build",
    "preview": "vp preview",
    "check": "vp check && svelte-check --tsconfig ./tsconfig.json",
    "format": "vp fmt .",
    "format:check": "vp fmt --check .",
    "generate-types": "node scripts/generate-api-types.mjs",
    "generate-types:check": "node scripts/generate-api-types.mjs && git diff --exit-code src/lib/api-types.ts",
    "coverage": "npm run coverage:frontend && npm run coverage:backend",
    "coverage:backend": "node scripts/run-coverage.mjs backend",
    "coverage:frontend": "vp test --coverage --run",
    "test:unit": "vp test",
    "tauri": "tauri",
    "tauri:dev": "npm run tauri -- dev",
    "tauri:start": "npm run build && npm run tauri -- dev --no-dev-server",
    "install:cli": "./scripts/install-cli.sh",
    "generate-manifests": "node scripts/generate-manifests.js"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3",
    "@sveltejs/kit": "^2",
    "@sveltejs/vite-plugin-svelte": "^7",
    "@tauri-apps/cli": "^2.11.2",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^24",
    "@vitest/coverage-v8": "^4.1.8",
    "esbuild": "^0.28.0",
    "js-yaml": "^5.0.0",
    "svelte": "^5",
    "svelte-check": "^4",
    "typescript": "^6",
    "vite-plus": "^0.1.24"
  },
  "overrides": {
    "vite": "npm:@voidzero-dev/vite-plus-core@latest",
    "vitest": "npm:@voidzero-dev/vite-plus-test@latest"
  },
  "packageManager": "npm@11.16.0"
}
